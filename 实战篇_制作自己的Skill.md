# 实战篇：制作自己的 Skill

最近研究了一下 Skill 系统，发现这东西挺有意思的。简单来说，Skill 就是给 Claude 装备"专业技能包"——把通用助手变成某个领域的专家。

## 核心设计原则

我理解了几个关键点：

### 简洁至上很重要
上下文窗口是有限的资源。Claude 本来就很聪明，不需要什么都教。写 Skill 的时候要不断问自己：这信息真的必要吗？值得消耗 token 吗？

### 自由度要匹配任务
不同任务需要不同的控制程度：
- 写博客文章给高自由度（多种写法都行）
- API 调用给中等自由度（有推荐模式但允许变化）
- 数据库操作给低自由度（必须按固定步骤）

### 分层加载很聪明
Skill 有三级加载机制：
1. 元数据常驻内存（100词左右）
2. 主体内容按需加载（<5k词）
3. 额外资源只在需要时加载

这样既节省上下文，又保证功能完整。

## Skill 文件结构

每个 Skill 至少需要一个 SKILL.md 文件：

```
skill-name/
├── SKILL.md          # 必需，包含配置和指令
├── scripts/          # 可选，可执行代码
├── references/       # 可选，参考资料
└── assets/           # 可选，输出资源
```

SKILL.md 分两部分：
- **前置元数据**（YAML格式）：name 和 description，告诉 Claude 什么时候用这个 Skill
- **主体内容**（Markdown）：具体的使用指南

## 实战案例

我试着用小红书内容创作作为例子：

### 第一步：明确需求
要解决什么问题？写小红书笔记需要标题、正文、标签，还要有互动性。

### 第二步：规划内容
哪些东西可以复用？标题生成公式、正文结构模板、标签选择策略……

### 第三步：编写配置

```yaml
---
name: xiaohongshu-automation
description: 小红书内容创作专家，擅长创作吸睛笔记。使用二级标题法、清晰结构和有效标签策略。
---
```

### 第四步：组织内容
我按照这个顺序组织：
1. 核心原则（标题怎么写、内容怎么排）
2. 工作流程（从需求到发布）
3. 实践规则（DO/DON'T）
4. 具体示例
5. 质量检查清单

### 第五步：分层处理
基础内容放 SKILL.md，详细分析放 references/。这样不会把主体搞得太长。

## 质量检查要点

我创建完后会检查：
- [ ] 元数据描述清晰，能让人知道什么时候用
- [ ] 主体内容不超过 500 行
- [ ] 有具体示例，不只是理论
- [ ] 指令直接明了，不啰嗦
- [ ] 不需要的不写（比如 README）

## 实用建议

根据我的经验：

1. **先看具体例子** - 别一上来就想通用方案，先解决具体问题
2. **能省则省** - Claude 已经会的就不用写
3. **按需加载** - 详细内容放 references，需要时再读
4. **多给例子** - 理论说半天不如一个实际案例
5. **持续改进** - 用了之后发现问题再改

## 配图和自动发布怎么实现？

这部分我在项目里看到了：

### 配图实现方案
1. **AI 生成图片** - 调用 DALL-E、Midjourney 等 API
2. **模板图片** - 准备好封面模板和内容图模板
3. **自动选择** - 根据正文内容智能匹配图片

项目里的 `images/` 目录放了一些模板图片，`assets/system_templates/` 下有更多展示案例。

### 自动发布实现方案
项目用的是 Playwright 做浏览器自动化：

1. **登录环节** - 模拟真实用户登录小红书
2. **填写表单** - 自动填入标题、正文、标签
3. **上传图片** - 上传封面图和内容图
4. **定时发布** - 用 Quartz 定时任务控制发布时间

代码在 `src/main/java/com/xhs/` 下，核心是 `PublishController` 和 `BrowserService`。

需要注意的反检测机制：
- 修改 User-Agent
- 模拟真实浏览行为
- 控制发布频率
- 使用代理 IP

这些都在项目文档里有详细说明。